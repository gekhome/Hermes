@using System.Collections
@using Hermes.Models
@using Hermes.DAL
@model int

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(true)
    .DataValueField("CHILD_ID")
    .DataTextField("FULLNAME")
    .OptionLabel("Επιλογή...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetParentChildren", "Parents");
        })
        .ServerFiltering(true);
    })
)
@Html.ValidationMessageFor(m => m)